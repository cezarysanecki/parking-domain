meta {
  name: View free time slots to request
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/requesting/view/free
  body: none
  auth: none
}

tests {
  var parkingSpotId = res.body[0].parkingSpotId;
  
  bru.setEnvVar("parkingSpotId", parkingSpotId);
}
